export const API=(import.meta.env.VITE_API_URL||'http://localhost:5000').replace(/\/$/,'')+'/api'; export async function api(path,opts={}){const h=opts.body instanceof FormData?{}:{'Content-Type':'application/json'};const token=localStorage.getItem('token');if(token)h.Authorization=`Bearer ${token}`;const res=await fetch(API+path,{...opts,headers:{...h,...opts.headers}});const data=await res.json().catch(()=>({}));if(!res.ok)throw Error(data.message||'Request failed');return data}