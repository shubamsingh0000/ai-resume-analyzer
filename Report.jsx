import {useEffect,useState} from 'react';import {useParams,Link} from 'react-router-dom';import {Download,ArrowLeft} from 'lucide-react';import {BarChart,Bar,XAxis,YAxis,ResponsiveContainer,Tooltip} from 'recharts';import Layout from '../components/Layout';import {api} from '../lib/api';export default function Report(){const {id}=useParams();const [a,setA]=useState();useEffect(()=>{api('/resumes/'+id).then(d=>{if(d.resume.analysis)setA(d.resume.analysis);else api('/resumes/'+id+'/analyze',{method:'POST'}).then(x=>setA(x.analysis))})},[id]);if(!a)return <Layout><p>Loading report...</p></Layout>;const data=Object.entries(a.breakdown||{}).map(([name,value])=>({name,value}));return <Layout><Link to="/" className="text-slate-500 flex gap-2"><ArrowLeft size={16}/> Dashboard</Link><div className="flex justify-between items-end mt-6"><div><p className="text-brand font-semibold">ANALYSIS REPORT</p><h1 className="text-4xl font-bold mt-2">Your resume score</h1></div><button onClick={()=>window.print()} className="btn border flex gap-2"><Download size={16}/> Download</button></div><div className="grid md:grid-cols-3 gap-5 mt-8"><div className="card p-6 md:col-span-1"><p className="text-slate-500">ATS compatibility</p><div className="text-6xl font-bold text-brand mt-4">{a.score}<span className="text-2xl text-slate-400">/100</span></div><p className="mt-4 text-slate-600">{a.summary}</p></div><div className="card p-6 md:col-span-2"><h2 className="font-bold mb-3">Score breakdown</h2><ResponsiveContainer width="100%" height={220}><BarChart data={data}><XAxis dataKey="name"/><YAxis domain={[0,100]}/><Tooltip/><Bar dataKey="value" fill="#635bff" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="grid md:grid-cols-2 gap-5 mt-5"><section className="card p-6"><h2 className="font-bold text-lg">What?s working</h2><ul className="mt-4 space-y-3">{a.strengths?.map(x=><li key={x} className="text-slate-600">? {x}</li>)}</ul></section><section className="card p-6"><h2 className="font-bold text-lg">Improve next</h2><ul className="mt-4 space-y-3">{a.improvements?.map(x=><li key={x} className="text-slate-600">? {x}</li>)}</ul></section></div></Layout>}